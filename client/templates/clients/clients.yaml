{{- if .Values.yuuvis.client.clients.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{.Values.yuuvis.labels.app}}
    name: clients
  name: clients
spec:
  replicas: {{.Values.yuuvis.client.clients.instances}}
  selector:
    matchLabels:
      name: clients
  template:
    metadata:
      labels:
        name: clients
    spec:
      automountServiceAccountToken: false
      {{- if or .Values.yuuvis.client.clients.nodeAffinity .Values.yuuvis.client.clients.podAffinity .Values.yuuvis.client.clients.podAntiAffinity }}
      affinity:
        {{- with .Values.yuuvis.client.clients.nodeAffinity }}
        nodeAffinity: {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.yuuvis.client.clients.podAffinity }}
        podAffinity: {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.yuuvis.client.clients.podAntiAffinity }}
        podAntiAffinity: {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- end }}
      initContainers:
      - name: clients-add-required-resources
        image: {{.Values.yuuvis.image.dockerRegistry}}/{{.Values.yuuvis.client.clients.initContainerImage}}
        imagePullPolicy: IfNotPresent
        command: [ 'sh', '/yuuvis/data/script/addresources.sh' ]
        env:
        - name: YUUVIS_TENANT
          valueFrom:
            secretKeyRef:
              key: tenant
              name: {{ .Values.yuuvis.init.configuser.secretname }}
        - name: YUUVIS_USER
          valueFrom:
            secretKeyRef:
              key: user
              name: {{ .Values.yuuvis.init.configuser.secretname }}
        - name: YUUVIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: {{ .Values.yuuvis.init.configuser.secretname }}
        {{- if .Values.yuuvis.common.env }}
        {{- toYaml .Values.yuuvis.common.env | nindent 8 }}
        {{- end }}
        volumeMounts:
        - mountPath: /yuuvis/data/script
          name: configureapps
        - mountPath: /yuuvis/data/resources
          name: clientsresources
        - mountPath: /tmp
          name: tmp
      containers:
      - image: {{.Values.yuuvis.image.dockerRegistry}}/{{.Values.yuuvis.client.clients.dockerImage}}
        imagePullPolicy: {{ .Values.yuuvis.image.imagePullPolicy }}
        name: client
        ports:
        - containerPort: 80
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        {{- if .Values.yuuvis.common.env }}
        env:
        {{- toYaml .Values.yuuvis.common.env | nindent 8 }}
        {{- end }}
        {{- if .Values.yuuvis.client.clients.resources }}
        resources: {{- toYaml .Values.yuuvis.client.clients.resources | nindent 10 }}
        {{- end }}
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      imagePullSecrets:
      - name: {{.Values.yuuvis.image.imagePullSecret }}
      restartPolicy: Always
      {{- with .Values.yuuvis.client.clients.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.yuuvis.client.clients.priorityClassName }}
      priorityClassName: {{ .Values.yuuvis.client.clients.priorityClassName }}
      {{- end }}
      {{- with .Values.yuuvis.client.clients.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroup: {{.Values.yuuvis.securityContext.fsGroup}}
        runAsGroup: {{.Values.yuuvis.securityContext.runAsGroup}}
        runAsUser: {{.Values.yuuvis.securityContext.runAsUser}}
        runAsNonRoot: {{.Values.yuuvis.securityContext.runAsNonRoot}}
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - emptyDir: {}
        name: tmp
      - configMap:
          name: clientsinit
        name: configureapps
      - configMap:
          name: clientsresources
        name: clientsresources
{{- end }}      