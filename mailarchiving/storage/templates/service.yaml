apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{.Values.yuuvis.labels.app}}
    name: mailarchiving-storage
  name: mailarchiving-storage
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.yuuvis.mailarchiving.storage.port }}
  selector:
    name: mailarchiving-storage
  type: ClusterIP