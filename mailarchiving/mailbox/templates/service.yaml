apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{.Values.yuuvis.labels.app}}
    name: mailarchiving-mailbox
  name: mailarchiving-mailbox
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{.Values.yuuvis.mailarchiving.mailbox.port}}
  selector:
    name: mailarchiving-mailbox
  type: ClusterIP