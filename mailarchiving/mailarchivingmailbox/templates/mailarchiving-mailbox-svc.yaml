apiVersion: v1
kind: Service
metadata:
  labels:
    app: yuuvis
    name: mailarchiving-mailbox
  name: mailarchiving-mailbox
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8110
  selector:
    name: mailarchiving-mailbox
  type: ClusterIP